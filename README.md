# GenLayer Multi-Source Football Oracle

GenVM-compatible Intelligent Contract for resolving football match outcomes using AI verification from trusted sources.

## Setup & Testing Instructions

Run the following commands to install dependencies and run unit tests:

```bash
pip install genlayer-sdk
python -m unittest test_football_oracle.py
